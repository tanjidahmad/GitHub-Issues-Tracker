1️⃣ What is the difference between var, let, and const?

'''var: var is the old way of declaring variables.It is function scope.

'''let: let is the block scoped. It can be updated but can not redeclared in the same blocked.

''''const: It is also blocked scope but it can not be updated or redeclared.

2️⃣ What is the spread operator (...)?

''' The spread operator is used mainly to expand elements of an array or properties of an objects.It help copy or merge data easily.

3️⃣ What is the difference between map(), filter(), and forEach()?

'''map() :create a new array. change each iteam of the array.

'''filter(): creates a new array .Returns iteams   that match a condition.

'''forEach(): runs a funtion for every element but it does not return a new array.

4️⃣ What is an arrow function?
...It is a shoter way to write a function in js.it makes the code cleaner and easier to read.




5️⃣ What are template literals?

...template literals are used to create string with embedded variables.They use backtickes instead of quotes.this is make it easier to combine text and variable.

